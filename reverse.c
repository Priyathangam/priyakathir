#include<stdio.h>
#include<conio.h>
void main()
{
    char str,strrev,temp;
    int i,n;
    printf("\nenter the string:",str);
    scanf("%d",&str);
    while(strrev==0)
    {
        strcpy(n,i);
        strcpy=strrev;
        }
    printf("\nthe reversed string is:");
    puts(strrev);
    getch();
}
